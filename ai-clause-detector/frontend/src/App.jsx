// src/App.jsx (Corrected Test Version)

// This will print to the browser console when the app loads
console.log("VITE_API_URL from App.jsx:", import.meta.env.VITE_API_URL);

export default function App() {
  // This will now display our test heading on the page
  return (
    <div>
      <h1>ClauseIQ - DEPLOYMENT TEST - It Works!</h1>
    </div>
  );
}
