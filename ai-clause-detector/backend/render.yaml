services:
  - type: web
    name: clauseiq-backend
    env: python
    runtime: python-3.11.9
    buildCommand: |
      pip install --upgrade pip
      pip install --only-binary=pydantic -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
